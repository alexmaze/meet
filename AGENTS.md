# Meet 项目协作说明

开始工作前先阅读：

1. `README.md`
2. `docs/product-requirements.md`
3. `docs/architecture.md`
4. `docs/decisions/` 中状态为 Accepted 的决策

## 已确认约束

- 本项目是私有部署、供家庭内部使用的多账号 Web 应用，不开放公众注册或多租户服务。
- 各家庭成员独立登录，对话、记忆和个人资料默认按账号隔离。
- 账号类型包括管理员、成人和儿童；儿童与成人使用相同的视觉和交互结构，但服务端权限不同，不能修改供应商等系统设置。
- 家庭成员账号只能由管理员创建，不提供邀请注册或家庭邀请码注册。
- 所有账号统一使用用户名和密码；每台设备只保持一个账号的登录状态，切换账号必须先退出。
- 管理员可以重置成员密码；管理员密码通过服务器命令重置，不接入邮箱找回。
- 儿童账号是否允许管理员查看历史按账号配置，默认允许；成人账号的私人对话、记忆和录音不能被管理员读取。
- 预置角色与管理员创建的角色全家共享；其他账号创建的角色默认私有，可以主动共享。
- 儿童只能使用家庭公共角色，不能创建、编辑或共享角色。
- 私人对话记忆保持隔离；家庭成员可以通过显式的家庭资料共享称呼、关系和共同信息。
- 只有管理员可以创建、编辑或删除家庭资料；成人和儿童只能在角色对话中读取使用。
- 与角色通话默认延续历史关系，同时提供不写入长期记忆的临时对话模式。
- 私人记忆中明确、可信的事实可以自动保存；不确定推断只能保存为待确认建议。
- 原始语音默认不长期保存，但用户可以按次开启录音，或在通话结束时选择保留临时录音。
- 角色创建同时支持自然语言 AI 生成和结构化高级编辑。
- 角色设定使用结构化角色卡，不把所有信息塞进一个不可解释的自由文本提示词。
- 第一版视觉范围是静态头像加聆听、思考、说话等轻量状态动画；不实现 Live2D、实时数字人或口型同步。
- 头像可以使用内置图片、用户上传图片或根据角色卡由 AI 生成。
- 第一版支持带版本号的角色卡导入导出；不得在角色包中包含对话、记忆、录音、凭据或其他账号数据。
- 三个预置角色首次部署后直接可用；允许管理员复制修改，并保留恢复原始版本的能力。
- 手机和平板是主要终端，桌面浏览器需要兼容。
- 第一版必须提供可安装的 PWA 和全屏启动体验，但实时聊天始终需要联网。
- 不设置应用级全局并发通话上限；仍需正确处理服务器资源和供应商配额错误。
- 第一版不实现 Web Push、定时提醒或角色主动来电。
- PWA 后台通话采用尽力保持策略；浏览器暂停连接时，返回前台后自动恢复。
- 网络中断后在约 30 秒内自动重连，恢复上下文和已完成记录，并避免重复保存消息。
- 模型服务失败时只重试当前模型；持续失败则暂停并让用户选择重试或结束，不静默切换模型、供应商或声音。
- 云端大模型是主要推理方式。
- 前后端统一使用 TypeScript。
- 使用 PostgreSQL 保存结构化数据，媒体资源使用独立文件目录或对象存储。
- 使用 Monorepo，至少包含 Web、API 和共享协议包；后台任务进程按实际需要增加。
- Web 使用 React + Vite + `vite-plugin-pwa`，不引入 SSR 框架。
- API 使用 Fastify；只有需要服务端 WebSocket 中继时才启用 `@fastify/websocket`，浏览器直连供应商的 WebRTC 不绕行 API。
- PostgreSQL schema 和迁移使用 Drizzle ORM；复杂查询可以使用参数化原生 SQL。
- Monorepo 使用 pnpm workspace。
- Web/API 共享协议以 Zod schema 为运行时事实来源，由 schema 推导 TypeScript 类型。
- 后台任务使用 `pg-boss` 与现有 PostgreSQL，不增加 Redis；第一版不引入额外队列管理面板。
- 所有媒体读写经过 MediaStore；提供本地目录和 S3 兼容实现，业务层不得直接拼接本地路径或对象存储 URL。
- PWA 发现新版本时提示用户刷新，不得在活跃通话中自动重载页面。
- 第一版的最高优先级是自然的实时角色聊天，不是功能数量。
- 第一版先使用模型供应商的预设声音，后续再加入克隆音色。
- 每个角色拥有固定声音，共享角色在所有账号中保持同一声音，不提供个人音色覆盖。
- 创建角色时由 AI 推荐 3 个预设音色，用户试听确认，并保留浏览全部音色的入口。
- 角色卡保存自然语言声音风格；会话中的临时语速或情绪要求不自动改写角色默认配置。
- 通话页默认采用沉浸式体验，并允许教学类角色展开字幕与辅助内容。
- 语音输入默认免提，使用语义轮次判断；必须保留按住说话的备用模式。
- 打断默认使用语义判断，尽量忽略附和声和环境噪声，并始终提供手动停止按钮。
- 普通角色默认只显示当前一句字幕；完整记录可展开，教学角色默认展开字幕和辅助区。
- 首页以角色卡为主，顶部优先展示最近通话和收藏角色。
- 所有账号的角色卡都提供独立通话按钮，点击其他区域进入详情；儿童不使用特殊的整卡通话行为。
- 手机使用角色、历史、记忆、我的底部导航，桌面转换为侧边栏；管理员功能放在我的页面内，不维护第二套后台前端。
- 视觉使用中性基础设计并按角色应用主题色、背景和氛围，不把整个产品固定为科幻或卡通风。
- 通话页以大幅静态角色立绘为中心，使用模糊延展背景、悬浮当前字幕和底部固定控制栏。
- 开场行为按角色配置，默认由角色先自然打招呼。
- 用户沉默约 10–15 秒后角色最多自然追问一次；没有回应时继续安静等待。
- 回复长度按角色与语境动态调整，陪伴角色偏简短，教学角色适合分步骤解释，并接受用户在会话中的即时调整。
- 第一版支持通话中上传图片或拍照，但不支持持续实时摄像头。
- 聊天图片默认随所属会话保存，可以单独删除；删除会话时一并删除其图片附件。
- 数学老师辅助区展示题目图片、Markdown/LaTeX 公式、分步讲解卡片和最终结论；第一版不做互动白板。
- 第一版只实现计算器工具，工具调用层应可扩展，但不要提前加入联网搜索、天气等工具。
- 默认技术验证对象是 `qwen-audio-3.0-realtime-plus`：浏览器使用 WebRTC，免提默认使用 `smart_turn`，默认音色为 `longanqian`；其供应商上下文最多保留 50 轮、累计 300 秒音频。
- 千问 WebRTC 是白名单能力，必须把商务提供的 Endpoint 配置为 `QWEN_REALTIME_ENDPOINT`，不能通过 Workspace ID 推导；服务端代理 SDP 时自行追加固定路径和模型参数。
- 千问 WebRTC 不支持 `turn_detection: null` 或 `input_audio_buffer.commit` 手动模式；界面“按住说话”只控制本地 RTP 音轨，松开后仍由 `smart_turn` 判断轮次。
- Qwen-Audio 只支持文本和音频输入，不支持图片；图片与教学多模态流程保留 `qwen3.5-omni-plus-realtime` 或独立视觉分析回退，豆包 S2S-SC 仍是重点语音对照组。
- 供应商 API 密钥不得进入浏览器代码、日志或版本库。
- 对话、角色和长期记忆默认保存在本地部署环境；只向云端发送完成当前推理所需的内容。
- 应用本身不实现全量备份或恢复功能，部署运维依赖云服务器磁盘快照；角色卡导出不视为完整备份。
- 云平台、域名、HTTPS、快照策略和生产运维由项目所有者自行处理。除非用户另行明确要求，不要替其选购云资源或执行生产部署。

## 工作约定

- 未经讨论，不要把应用永久绑定到某一个实时模型供应商。
- 修改已经接受的产品或架构决策时，新建或更新 `docs/decisions/` 下的决策记录。
- 尚未确定的事项保持为“待定”，不要从现有项目或个人偏好中猜测。
- 涉及外部 API、SDK 或模型能力时，使用当前官方文档核对，不依赖旧版记忆。
- 新增环境变量时同步维护 `.env.example`，但绝不写入真实密钥。
- 面向用户的默认文案和项目文档使用简体中文。
