# Meet

Meet 是一个私有部署、供家庭内部使用的 AI 实时语音角色扮演 Web 应用。

用户可以和具有稳定人设、自然声音与长期记忆的 AI 角色实时交谈，例如：

- 孩子与英雄角色聊天；
- 向数学老师请教问题；
- 成年人与影视风格角色谈心；
- 创建自己的陪伴、教学或娱乐角色。

## 当前状态

项目已经完成需求基线、技术栈选择和第一版千问 WebRTC 技术样例，现已进入 MVP 业务开发阶段。当前代码可以启动 React PWA、Fastify API，并通过服务端 SDP 握手代理连接 `qwen-audio-3.0-realtime-plus`；免提模式默认使用 `smart_turn`，默认音色为 `longanqian`。第一版默认继续使用这个模型实现完整业务闭环，同时保留 Provider Adapter，不把角色、会话和记忆永久绑定到千问。账号数据库基础、首位管理员初始化、用户名密码登录、家庭成员管理，以及由三个预置角色、结构化角色卡、角色权限、角色首页和实时通话组成的首个角色业务纵向切片已经实现；会话、消息、记忆和媒体持久化仍待继续开发。真实凭据与手机、桌面设备实测和业务开发并行，不再阻塞需求推进。

已经确认的方向：

- 私有部署、家庭内部使用，不开放公众注册；
- 支持多个家庭成员独立登录，各自的对话历史和记忆相互隔离；
- 账号分为管理员、成人和儿童；儿童使用与成人相同的界面，仅按权限隐藏不能执行的操作；
- 家庭成员只能由管理员创建，所有账号统一使用用户名和密码；
- 每台设备只保持一个登录账号，切换成员时需要先退出再登录；
- 管理员可以重置成员密码；管理员密码通过服务器命令重置；
- 儿童账号的历史可见范围按账号配置，默认管理员可查看；成人账号的私人内容对管理员不可见；
- 主要在中国大陆使用，服务部署在国内云服务器；
- 具体云平台、域名、HTTPS、快照和生产运维由项目所有者自行处理，不纳入应用产品范围；
- 手机和平板优先，桌面浏览器兼容；
- 支持安装为全屏 PWA，但实时聊天仍需联网；
- 应用不设置全局并发通话上限，容量由部署环境和模型供应商额度决定；
- 第一版不提供推送、定时提醒或角色主动来电；
- PWA 进入后台时尽力保持通话，被系统暂停后在回到前台时自动恢复；
- 网络中断后自动重连最多约 30 秒，并恢复上下文与已完成记录；
- 模型故障只重试当前模型，持续失败时暂停通话，不静默更换模型或声音；
- 推理和语音主要使用云端模型；
- 前后端统一使用 TypeScript；
- 数据使用 PostgreSQL，媒体文件放在独立文件目录或对象存储；
- 代码采用 Monorepo，至少拆分 Web、API 和共享协议包，并按需增加后台任务进程；
- Web 使用 React、Vite 和 `vite-plugin-pwa`；
- API 使用 Fastify，必要的服务端长连接使用 `@fastify/websocket`；
- PostgreSQL 访问与迁移使用 Drizzle ORM，同时允许复杂查询直接写 SQL；
- Monorepo 使用 pnpm workspace；
- Web/API 共享协议使用 Zod 做运行时校验；
- 后台任务使用 `pg-boss` 和现有 PostgreSQL，不引入 Redis；
- 媒体通过统一 MediaStore 访问，默认本地目录，也支持 S3 兼容对象存储；
- 第一版优先自然、低延迟、有角色感的实时语音聊天；
- 第一版是通用角色平台，预置儿童英雄、数学老师和成人情绪陪伴三类角色；
- 三个预置角色首次部署后即可通话，管理员可以复制修改，并可恢复原始版本；
- 支持用自然语言让 AI 快速生成角色，并通过结构化高级角色卡继续编辑；
- 第一版使用静态头像和轻量状态动画，不做 Live2D 或数字人口型；
- 角色头像支持内置图片、用户上传和根据角色卡 AI 生成；
- 角色卡可以导入导出，但不包含私人对话、记忆或录音；
- 预置及管理员创建的角色全家共享，个人角色默认私有并可主动共享；
- 儿童只能使用家庭公共角色，不能创建角色；
- 私人记忆保持隔离，同时可以维护一份明确的家庭资料；
- 家庭资料仅管理员可以编辑，成人和儿童只能在角色对话中读取使用；
- 与同一角色默认延续关系和记忆，也可以发起不写入长期记忆的临时对话；
- 明确事实可自动进入私人记忆，存疑内容需要用户确认；
- 原始通话录音默认不长期保存，可以按次开启或在结束时选择保留；
- 通话页采用自适应形态：默认沉浸式，教学等角色可以展开字幕和辅助内容；
- 首页以角色卡为主，突出最近通话和收藏；所有账号的角色卡都使用独立通话按钮；
- 手机使用角色、历史、记忆、我的底部导航，桌面自适应为侧边栏；
- 通话默认免提并使用语义轮次与语义打断，嘈杂环境可以切换为按住说话；
- 普通角色只显示当前一句字幕，完整记录可展开；教学角色默认展开字幕和辅助区；
- 角色开场行为可以配置，默认由角色自然打招呼；
- 用户沉默约 10–15 秒后角色可以自然追问一次，之后安静等待；
- 回复长度按角色和语境动态调整，并响应用户“简单点”或“详细点”等要求；
- 通话中支持上传图片或直接拍照；
- 聊天图片默认随历史记录保存，并可以单独删除；
- 数学老师辅助区支持题目图片、Markdown/LaTeX 公式和分步讲解卡片；
- 第一版工具调用只实现计算器，底层保留扩展接口；
- 第一版使用云端预设声音，后续支持克隆音色；
- 每个角色拥有全家一致的固定声音；AI 根据角色卡推荐 3 个预设音色，用户试听后确认；
- 角色卡保存语速、活力、温柔程度和情绪表现等声音风格，通话中可临时调整；
- 视觉采用简洁中性的基础界面，并按角色切换主题色、背景和氛围；
- 通话页使用大幅角色立绘、模糊延展背景、悬浮当前字幕和底部固定控制栏；
- 第一版默认使用千问 `qwen-audio-3.0-realtime-plus` 实现纯语音角色体验；
- Qwen-Audio 不支持图片输入，拍题和教学图片保留 Qwen3.5 Omni Plus Realtime 或独立视觉分析回退；
- 使用豆包 S2S-SC 做中文角色表现的对比测试；
- 从第一天保留模型供应商和音色的可替换边界；
- 应用不实现完整数据备份，部署时依赖云服务器磁盘快照。

## 项目文档

- [产品需求](docs/product-requirements.md)
- [技术架构草案](docs/architecture.md)
- [实时语音模型调研](docs/research/realtime-models-2026-08-08.md)
- [已确认的产品与模型决策](docs/decisions/0001-product-and-model-strategy.md)
- [业务开发与第一版默认实时模型决策](docs/decisions/0025-business-development-and-default-realtime-model.md)
- [Qwen WebSocket PCM 与确定性打断决策](docs/decisions/0026-qwen-websocket-pcm-playback.md)

## 本地运行

环境要求：Node.js 22.12 或更高版本、pnpm 10。

```bash
cp .env.example .env
pnpm install
pnpm db:migrate
pnpm admin:init --username admin --display-name 家庭管理员
pnpm dev
```

运行迁移前，先在 PostgreSQL 中创建空数据库，并在 `.env` 中填写对应的 `DATABASE_URL`。`admin:init` 只允许在没有任何账号的数据库中执行，密码通过终端遮罩输入两次，至少 15 个字符，不会进入命令历史。管理员忘记密码时可在服务器执行：

```bash
pnpm admin:reset-password --username admin
```

重置成功后，该管理员现有的全部登录会话都会被撤销。应用不提供公开注册；进入实时语音页面前必须先用已创建的账号登录。

示例配置为了本地 HTTP 开发使用 `AUTH_COOKIE_SECURE=false`；生产环境启用 HTTPS 后必须改为 `AUTH_COOKIE_SECURE=true`。

管理员登录后可以从“我的”页进入家庭成员管理，创建成人或儿童账号、重置成员密码，并单独配置管理员能否查看每个儿童账号的历史、记忆和已保留录音。管理员账号本身不能通过 Web 重置密码，仍需使用上面的服务器命令。账号停用与恢复的产品方式仍待确认，本版没有提前实现。

登录后的角色首页会直接显示星盾队长、林老师和知夏三个家庭预置角色。点击角色卡的“通话”按钮会使用该角色已经保存的人设、声音和开场策略进入实时通话，点击卡片其他区域可以查看结构化角色卡。管理员和成人可以创建、编辑、复制与删除自己的角色；管理员创建的角色默认全家共享，成人创建的角色默认私有并可主动共享，儿童只能查看和使用家庭公共角色。预置角色只能由管理员修改和恢复，恢复不会删除任何成员与该角色的会话或记忆。

千问 WebRTC 当前是白名单能力。先联系阿里云商务获取专用 Endpoint，再在 `.env` 中填写：

```dotenv
DASHSCOPE_API_KEY=在服务端填写真实值
QWEN_REALTIME_ENDPOINT=商务提供的Endpoint主机名
```

Endpoint 推荐填写纯 hostname；也接受不带路径、查询参数或端口的 HTTPS origin。服务端会自行追加 `/api/v1/webrtc/realtime?model=...`，不能通过 Workspace ID 推导接入地址。然后访问 `http://localhost:5173`，API 默认监听 `http://127.0.0.1:8787`。API Key 和 Endpoint 只由 API 读取，不会返回浏览器。

从角色首页发起通话后，服务端会按角色的 Provider Profile 和 Voice Profile 校验实时模型，浏览器不再向握手接口自由提交模型。然后可以验证：

- 角色先开口与连续免提聊天；
- 实时用户/角色字幕；
- 用户开口时语义打断与手动停止；
- 仍由 `smart_turn` 收尾的本地按住说话备用模式；
- WebRTC、服务端 `txt` DataChannel 和供应商错误事件。

完整设置、协议说明和已知限制见 [千问 WebRTC 技术验证](docs/spikes/qwen-realtime-webrtc.md)。

## 下一步

1. 接入会话与消息持久化、历史记录、断线恢复和幂等保存，再继续长期记忆与后台任务。
2. 继续将当前千问链路收敛到 Provider Adapter 边界，并为后续图像能力与供应商对照保留清晰接口。
3. 在基础角色闭环稳定后补充 AI 快速创建、头像上传与生成、角色卡导入导出和音色试听，不让这些扩展阻塞会话持久化。
4. 单独确认管理员停用与恢复家庭账号的产品方式，再实现对应状态流转和会话撤销，不从当前数据库状态字段直接猜测交互。
5. 在业务开发期间并行完成手机与桌面设备实测、延迟和打断记录、图片能力路由，以及 Qwen-Audio Flash 与豆包对照；这些结果用于验收和后续默认模型调整，不再阻塞业务切片。
